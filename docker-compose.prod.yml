services:
  app:
    container_name: suganuma-ops-hub
    hostname: suganuma-ops-hub
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env.prod
    expose:
      - "3000"
    networks:
      - proxy

networks:
  proxy:
    external: true
