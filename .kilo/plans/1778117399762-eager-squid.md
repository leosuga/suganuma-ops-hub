# Plano Consolidado — Estado Atual, Gaps e Próximos Passos

> Data: 2026-05-06  
> Baseado nos planos anteriores: `lucky-engine` (correções/funcionalidades) e `happy-otter` (integridade/performance/modernização).

---

## Diagnóstico: o que já está feito

### Do plano `lucky-engine` (Correções + Funcionalidades)
| Item | Status |
|------|--------|
| 1.1 Protocol streak | ✅ Corrigido — `ProtocolList.tsx` percorre `sortedDates` e compara `expected` corretamente |
| 1.2 BiometricLog campo `mg_dl` | ✅ Alinhado — UI e schema usam `mg_dl` |
| 1.3 RevenueChart day extraction | ✅ Corrigido — usa `new Date(...).getUTCDate()` |
| 2.1 Edição de Task | ✅ `EditTaskDialog.tsx` existe e funciona |
| 2.2 CRUD de Contas | ✅ `AccountManager.tsx` existe, queries `useUpdateAccount`/`useDeleteAccount` existem |
| 2.3 Edição de Protocolo | ✅ `useUpdateProtocol`/`useDeleteProtocol` usados em `ProtocolList.tsx` |
| 2.4 Optimistic delete transaction | ✅ `useDeleteTransaction` já tem `onMutate`/`onError` rollback |
| 3.1-3.3 Dashboard quick-adds | ✅ Task, despesa e peso rápidos já existem em `dashboard/page.tsx` |
| 4.1 Busca textual de tasks | ✅ Search bar com filtro client-side existe em `tasks/page.tsx` |
| 4.2 Notas da task | ✅ `EditTaskDialog` mostra textarea de notas |
| 4.4 Ordenação/urgência no Dashboard | ✅ Seção "Precisa de atenção" com tasks urgentes/atrasadas já existe |

### Do plano `happy-otter` (Integridade/Performance)
| Item | Status |
|------|--------|
| 1.1 MCP Server — notes/meals/habits | ❌ Faltam API routes + tools no MCP |
| 1.2 Schema tests meal/note/habit | ❌ Apenas 27 testes; meal/note/habit não cobertos |
| 1.3 `@tailwindcss/typography` | ❌ Não instalado; `prose`/`prose-invert` são no-ops |
| 1.4 `manifest.webmanifest` + ícones | ❌ Não existe; PWA não é instalável |
| 1.5 README rewrite | ❌ Ainda é boilerplate do `create-next-app` |
| 1.6 `npm test` script | ❌ Não existe |
| 2.1 Remover `lucide-react` | ❌ Instalado (v1.8.0) mas zero imports em `src/` |
| 2.2 Mover `shadcn` para `devDependencies` | ❌ Ainda em `dependencies` |
| 2.3 Node.js v22 no Dockerfile | ❌ Ainda `node:20-alpine` |
| 3.1 Realtime em notes/meals/habits | ⚠️ `notes` já tem; `meals` e `habits` **faltam** |
| 3.2 Regenerar `database.types.ts` | ❌ Tipos manuais; pode divergir das 10 migrations |
| 3.3 `strict: true` no tsconfig | ✅ Já está habilitado (linha 7) |
| 3.4 `<title>` dinâmico nas rotas | ❌ Nenhuma rota tem `metadata` exceto `layout.tsx` genérico |
| 3.5 `loading.tsx`/`error.tsx` | ❌ Nenhum existe em `src/app/(app)` |
| 3.6 ESLint (`debug` corrompido) | ❌ `npm run lint` pode falhar |
| 4.1 `staleTime`/`gcTime` TanStack | ❌ Não configurado; refetch imediato ao re-focus |

---

## Gaps extras encontrados (não listados nos planos anteriores)

| # | Tarefa | Motivo | Severidade |
|---|--------|--------|------------|
| E1 | **Bug: glicemia sem campo `fasting`** — `glucoseValueSchema` exige `fasting: z.boolean()`, mas `BiometricLog.tsx` não renderiza checkbox/input para `fasting`. Salvar glicemia falha na validação Zod. | Quebra funcionalidade real de saúde | **Alta** |
| E2 | **`next.config.ts` sem otimizações** — sem `experimental.optimizePackageImports` (útil para `recharts`, `cmdk`, `lucide-react` se ficar) ou `typedRoutes` | Bundle size / DX | Média |
| E3 | **`shadcn/tailwind.css` importado mas `@tailwindcss/typography` ausente** — Notes renderizam markdown cru sem estilo tipográfico | UX das notas | Média |
| E4 | **Coolify migration** — plano detalhado existe no `happy-otter` (seção 6) mas ainda não iniciado | Infra / deploy futuro | Baixa (não é regressão) |

---

## Prioridade Ordenada — O que fazer agora

### 🔴 Sprint A — Bugs e Correções Rápidas (impacto imediato)
1. **A1 — Bug glicemia `fasting` (E1)**  
   Adicionar campo `fasting` (checkbox) no `BiometricLog.tsx` quando `kind === "glucose"`. ~15 min.

2. **A2 — Realtime em `meals` e `habits` (3.1)**  
   Adicionar `useRealtimeTable("meal", mealKeys.all)` e `useRealtimeTable("habit_track", habitKeys.all)` nos hooks `useMeals`/`useHabits`. ~10 min.

3. **A3 — Script `npm test` (1.6)**  
   Adicionar `"test": "vitest --no-watch"` no `package.json`. ~2 min.

4. **A4 — Remover `lucide-react` (2.1) + mover `shadcn` para `devDependencies` (2.2)**  
   `npm uninstall lucide-react` + edit `package.json`. ~5 min.

### 🟡 Sprint B — Integridade e Tests (confiança no código)
5. **B1 — Schema tests: `mealSchema`, `noteSchema`, `habitTrackSchema`, `habitEntrySchema` (1.2)**  
   Adicionar testes em `tests/schemas.test.ts`. ~20 min.

6. **B2 — Instalar `@tailwindcss/typography` v4 (1.3)**  
   `npm install -D @tailwindcss/typography` e registrar `@plugin` no `globals.css`. Testar build. ~15 min.

7. **B3 — `manifest.webmanifest` + ícones PWA (1.4)**  
   Criar `public/manifest.webmanifest`, adicionar ícones mínimos (192x192, 512x512) ou SVG. Referenciar no `layout.tsx`. ~20 min.

8. **B4 — Health colors token (4.3 do `lucky-engine`)**  
   Substituir `#A8D8B0` hardcoded em `HealthTrends.tsx` por `stroke="var(--color-health)"` ou classe Tailwind `text-health`. ~10 min.

### 🟠 Sprint C — MCP Server e Documentação (maior trabalho, alto impacto)
9. **C1 — Completar MCP Server (1.1)**  
   Criar API routes:
   - `GET/POST/PATCH/DELETE /api/agent/notes`, `/api/agent/notes/[id]`
   - `GET/POST /api/agent/meals`, `/api/agent/meals/plans`
   - `GET/POST /api/agent/habits`, `/api/agent/habits/[id]/entries`
   - `GET /api/agent/health/appointments` (listagem)
   
   Depois registrar tools em `mcp-server/src/index.ts`:
   - `notes_list`, `notes_create`, `notes_update`, `notes_delete`
   - `meals_list`, `meals_create`, `meals_set_plan`
   - `habits_list`, `habits_create`, `habits_log_entry`
   - `health_list_appointments`
   
   ~1.5-2h.

10. **C2 — Reescrever `README.md` (1.5)**  
    Stack, arquitetura (Zod → Queries → Components → Page), deploy (VPS/Coolify), MCP Server, export/import. Depende de C1. ~30 min.

### 🟢 Sprint D — Performance, DX e Polish
11. **D1 — `staleTime`/`gcTime` global no QueryClient (4.1)**  
    Configurar `staleTime: 60_000` e `gcTime: 5 * 60_000` no `QueryClientProvider`. ~10 min.

12. **D2 — Dynamic `<title>` nas rotas (3.4)**  
    Adicionar `export const metadata = { title: "..." }` (ou `useEffect` + `document.title` para client components) em cada `page.tsx` de `/(app)`. ~15 min.

13. **D3 — `loading.tsx` nas rotas principais (3.5)**  
    Criar `src/app/(app)/tasks/loading.tsx`, `finance/loading.tsx`, `health/loading.tsx` com skeleton simples. ~15 min.

14. **D4 — Regenerar `database.types.ts` (3.2)**  
    Rodar `npm run types:supabase` se CLI linkado; senão, auditar manualmente contra as 10 migrations. ~20 min.

15. **D5 — `next.config.ts` modernizações (2.4, 2.6, E2)**  
    Adicionar `experimental.optimizePackageImports: ["recharts", "cmdk"]` e `@next/bundle-analyzer` como `devDependency` (opcional, condicional no build). Verificar se `typedRoutes` é viável. ~15 min.

### ⚪ Sprint E — Infra e Modernização (backlog futuro)
16. **E1 — Node.js v22 no Dockerfile (2.3)**  
    Trocar `node:20-alpine` → `node:22-alpine`. Validar build completo. ~20 min.

17. **E2 — React Compiler (2.5)**  
    Testar `experimental.reactCompiler: true` em branch separada. Só mergear se `next build` passar sem erros. ~30 min (investigativo).

18. **E3 — ESLint `debug` corrompido (3.6)**  
    `rm -rf node_modules && npm install` ou isolar a dependência. ~15 min.

19. **E4 — Coolify migration**  
    Plano detalhado já existe no `happy-otter` (seção 6). Requer acesso ao dashboard do Coolify e decisões do usuário (proxy Caddy vs Traefik). Não prioritário agora.

---

## Resumo visual por severidade

| Severidade | Quantidade | Itens |
|------------|-----------|-------|
| 🔴 Alta | 4 | A1 (glicemia), A2 (realtime meals/habits), C1 (MCP completion), A3+A4 (test script + deps) |
| 🟡 Média | 5 | B1-B4 (tests, typography, manifest, health token), D4 (database types) |
| 🟢 Baixa/Future | 10 | D1-D3 (staleTime, titles, loading), D5 (next.config), E1-E4 (Node v22, React Compiler, ESLint, Coolify) |

---

## Decisões pendentes do usuário

1. **Coolify migration**: quer iniciar agora ou deixar para depois que o app estiver 100% estável?  
2. **React Compiler**: quer que eu teste em branch separada?  
3. **Node.js v22 no Dockerfile**: aceita o risco de build? (o build atual funciona em v20)  
4. **Ícones PWA**: prefere gerar ícones automaticamente (ex: via `pwa-asset-generator`) ou usar SVG placeholder?  
5. **ESLint fix**: `npm install` resolve o `debug` corrompido? (pode ser só `node_modules` inconsistente)
